# Thresholds

## Data Thresholds

- Fixture schema must match `docs/order-schema.md`.
- Generated summary must be derived from same-round fixture data.

## Verification Thresholds

- Local runtime test output must be captured under the active round.
- Adversarial review must cite runtime evidence.
- Review must not accept the round if runtime criteria are vague or changed after execution.

