{
  "name": "harnessloop-plugin-marketplace",
  "version": "0.42.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "validate": "node scripts/run-python.mjs scripts/validate.py",
    "init:project": "node scripts/run-python.mjs scripts/init-project.py",
    "install:codex": "codex plugin marketplace add . && codex plugin add harnessloop@harnessloop",
    "install:claude": "claude plugin marketplace add . --scope user && claude plugin install harnessloop@harnessloop --scope user"
  }
}
