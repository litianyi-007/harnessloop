# Harnessloop 产品方反馈：对《当框架要求一切都有证据，除了它自己》的正式回应

> 回应基线：仓库 v0.9.0，commit `ac219a3`。
> 回应人：独立 CTO 兼 CEO（外部聘请，非原作者）。
> 日期：2026-07-02。
> 方法声明：本文每一项表态都基于对仓库文件的独立复核，不采信客户报告的任何未经核实的论断。所有结论附文件路径与行号。

---

## 一、总体立场（CEO 视角）

先说结论，不绕弯子：**这是我见过的对一个早期项目质量最高的一份外部评审。它对我们最有价值的地方，恰恰是它几乎没有说错。**

我用一整轮独立核实去找这份报告的破绽——按照 Harnessloop 自己第 13 条原则的标准，逐条回到文件里验证，而不是读完觉得"有道理"就签字。核实结果如下表。**结论是：报告列出的九项批评，没有一项是捏造或夸大到需要驳回的。**

作为对产品成败负责的人，我的整体判断分三层：

1. **诊断层：全盘接受。** 报告对"长任务 agent 六种死法"的复述、对"协议的强制力从哪里来"这一核心追问、对"信任悖论"的定性，都是准确且深刻的。这不是客套——它把我们自己没有系统化说清楚的结构性矛盾，一句话点破了：**我们在用"模型的自觉"去执行一套"防模型不自觉"的协议。** 这句话会被写进 1.0 的架构原则。

2. **事实层：绝大部分属实，两处需要更正。** 九项批评里，可机械核实的部分（无 LICENSE、跨平台断裂、硬编码个人路径、文档漂移、无升级机制、无使用证据）全部属实，我方逐条认账。仅有两处需要为记录更正（见下），且这两处更正**都不改变报告的结论**，只是让证据更精确——我把它们写出来，是为了证明我们真的回到文件里核对过，而不是礼貌性附和。

3. **决策层：报告只诊断，不决策；这是我的工作。** 报告给出的"出路是下沉"是正确方向，但它是一个观察者的建议，不是一个带优先级、验收标准和交付时间的路线。第三节我把接受的每一条转成 P0/P1/P2，落到"做/不做/何时做"。

**接受比例（正式记录）：**

| 类别 | 数量 | 项 |
| --- | --- | --- |
| 完全接受 | 7 / 9 | 不足 2、3、4、5、6；思辨 1、2 |
| 部分接受 | 2 / 9 | 不足 1（最小路径）；思辨 3（产品还是论文） |
| 不接受 | 0 / 9 | 无 |
| 事实性更正（不改变结论） | 2 处 | 模型名硬编码的确切出处；"五处拷贝"的修复方式 |

"不接受 0 项"不是软弱。我被聘为独立视角、没有为现状辩护的动机——如果报告有错，我会驳。它没有实质错误，所以正确的 CEO 动作是承认，然后把承认转成行动，而不是为了显示独立性去制造分歧。我提供的增量价值在别处：独立复核、两处更正、优先级排序、战略定位决策、以及 dogfooding 的启动决定。

### 独立核实结论（CTO 复核，逐条附证据）

| 报告指控 | 复核结论 | 仓库实据 |
| --- | --- | --- |
| 强制力真空：协议 gate 无机器强制 | **属实** | `scripts/validate.ps1` 全部断言仅覆盖 manifest 字段、init 产物存在性、secrets 值为 null（`validate.ps1:151-153`）；scope-lock（`harnessloop-loop/SKILL.md:343-351`）、只读 status、continuation gate 均为纯文字 |
| secrets 已下沉为可执行断言 | **属实** | `Invoke-HarnessloopSecretsSmoke`（`validate.ps1:115-154`）中 `if ($null -ne $Param.value) { throw }` 是全仓库唯一的协议级硬断言 |
| README 骨架缺 `intake/`、`evals/` | **属实** | `README.md:100-118` 无这两个目录，但 `init_project.py` `BASE_DIRS`（`:16-24`）两者皆建 |
| usage.md 初始化清单缺三份文件 | **属实** | `docs/usage.md:47-56` 缺 `state/current.md`、`state/self-check.md`、`evals/matrix.md`；三者均由 `BASE_FILES`（`init_project.py:29,33,35`）实际创建 |
| 无 LICENSE | **属实** | 全仓库 glob `**/LICENSE*` 无命中 |
| npm scripts 仅 Windows | **属实** | `package.json:5-10` 四条脚本全部直调 `powershell`；`scripts/` 下无 `validate.sh`，只有 `validate.ps1` |
| README / validate.ps1 硬编码个人路径 | **属实** | `README.md:239-250` 出现 `C:\Users\litianyi\.codex\...`；`validate.ps1:28` 出现 `C:\nvm4w\nodejs\claude.cmd` |
| 已装项目无升级机制 | **属实** | `init_project.py:68-77` skip-unless-force；生成文件无版本字段；无 migrate/diff 工具 |
| 无真实使用证据、无 dogfooding | **属实** | 仓库根无 `.harnessloop/`（glob 无命中）；唯一示例 `examples/mock-project/` 为人造；`evals/matrix.md` 仅"Sample value"一列 |
| 模型名烤死在协议里 | **属实但出处需更正** | `framework.md:133-134,292-293` 与 `SKILL.md:392-394` 确有 `gpt-5.5-medium`、`Sonnet`；但**安装到项目的** `cost-context-policy-template.md` 的 Model Policy 段是空字段（`:51-65`），并未硬编码 |

---

## 二、逐条回应

### 第四节 · 不足 1：仪式感过重，最小可用路径缺失

**接受程度：部分接受。**

**我方分析：** 两个论断要拆开。

- "整个仓库没有一份 5 分钟跑通最小循环的 walkthrough"——**完全属实，无可辩解。** README 的 "Start Your First Loop"（`README.md:120`）确实把七个协议步骤塞进一句话，且不展开任何一步。这是入口设计的失败。
- "仪式感过重"作为**缺陷**——**部分保留。** 对我们锁定的目标用户（高风险、多天、跨会话、需要完整审计轨迹的团队），这套仪式不是负担，而是产品本身；把它减薄到"日常编码也能用"会摧毁我们唯一的差异化。README 的 "Do not use it for"（`README.md:34-38`）已经明确劝退轻量用户，这是对的。所以问题不是"协议太重"，而是"重协议缺少轻入口，且没有告诉用户哪些环节可裁剪"。

**决策（做 / 何时做）：**
- **做**：P1 交付一份 `docs/quickstart.md`，用一个真实小目标走完"一目标 / 一轮 / 一份证据 / 一次评审 / 一个 decision"，全程 ≤10 步。
- **做**：P1 引入显式的三档 profile（`lite` / `standard` / `strict`），在协议文档里标注每一个 gate 在 `lite` 档是否可省，把"哪些能裁剪"从隐性判断变成成文策略。
- **不做**：不削减 `standard`/`strict` 档的协议密度。理由：那是目标用户买单的东西。

### 第四节 · 不足 2：强制力真空（客户称"最硬伤"）

**接受程度：完全接受（诊断与出路都接受；仅澄清一处成因）。**

这是整份报告最重要的一条，也是客户点名要求 CTO 正面回答的一条：**secrets 已经证明下沉可行，为什么其余 gate 没有同样下沉？是否改变路线？**

我正面回答。

**为什么 secrets 先被下沉？** 三个原因，两个正当、一个不正当：
1. （正当）它是纯机械可判的：`value` 字段是否为 null 是一个语法级检查，不需要任何判断。
2. （正当）它的失败是不可逆且灾难性的：一个泄露的密钥提交进 git 无法真正撤回。安全项理应第一个上硬约束。
3. （**不正当**）其余 gate 之所以还停留在文字，一部分是因为它们确实含判断成分（"这份证据是否支持验收？"——这本就该留给模型），但另一部分**纯粹是因为我们还没做把"机械子集"从"判断子集"里切出来的工作**。这是我方的欠账，不是设计选择。

客户的关键洞察我完全接受：**协议规则可以二分。** 判断类（证据是否支持验收、目标是否漂移）留给模型是对的；机械可判类（写入路径是否在 scope-lock 白名单内、评审引用的证据路径是否存在、只读 status 是否产生了文件变更、状态文件是否内部自洽）留给模型就是浪费信任。secrets 证明了下沉的可行性，其余机械规则没有任何理由不照做。

**是否改变路线？** 改变的是**架构承诺**，不是**产品定位**：
- 定位不变：仍然协议优先（理由见第四节战略部分——纯协议换来了 Codex/Claude Code 双运行时便携，这是护城河）。
- 架构改变：1.0 确立一条硬原则——**"代码保证协议的下限，模型只负责协议的上限"**（这句话直接借用客户报告，我们认可它就是对的）。P0 交付这条原则的第一个落地：一个跨平台的 `harnessloop verify`，最小覆盖"scope-lock 路径越界"和"评审引用证据是否存在"两条机械 gate。

**决策（做 / 何时做）：**
- **做（P0）**：`harnessloop verify` MVP，跨平台，纳入 CI。验收标准见第三节。
- **做（P1+）**：逐步把 status 只读性（零文件变更）、状态文件一致性等机械规则纳入 `verify`。
- **不做**：不把判断类 gate 伪装成脚本。理由：假的机器强制比诚实的文字规范更危险。

### 第四节 · 不足 3：文档五处拷贝，漂移已经发生

**接受程度：完全接受漂移事实；修复方式我方有更精确主张。**

**我方分析：** 漂移是现状，已核实（见上表两行）。一个把 "validation drift" 写进自审清单（`framework.md:256`、`SKILL.md:307`）的框架，自己的文档先漂移了——这个讽刺我方照单全收。

**一处更正（不改变结论）：** 客户把问题表述为"同一套骨架重复了五遍"，隐含"重复=缺陷、应删重复"。我不接受这个隐含推论。README（面向上手/营销）、`framework.md`（面向规格）、`SKILL.md`（面向模型运行时）服务的是三类不同读者，不能塌缩成一个文件。**真正的缺陷不是重复，而是没有单一事实源（single source of truth）来校验这些副本。** 可执行的真相已经在 `init_project.py` 的 `BASE_DIRS`/`BASE_FILES` 里——修复方向是让它成为权威源，用 CI 校验所有文档骨架与它一致，而不是删掉面向不同读者的文档。

**决策（做 / 何时做）：**
- **做（P0）**：以 `init_project.py` 常量为骨架的唯一权威源；新增校验步骤，任何文档骨架与之偏离即 CI 失败；同步修好 README（补 `intake/`、`evals/`）与 usage.md（补 `current.md`、`self-check.md`、`matrix.md`）。
- **做（P0）**：把"文档一致性"纳入 `validate`——直接回应客户"validate.ps1 检查 manifest 和 smoke test，不检查文档一致性"这一精准批评。

### 第四节 · 不足 4：工程成熟度信号缺失

**接受程度：完全接受。逐项无异议。**

| 子项 | 复核 | 决策 |
| --- | --- | --- |
| 无 LICENSE | 属实（glob 无命中） | **做（P0）**：加 Apache-2.0（理由见下） |
| 跨平台名不副实 | 属实（`package.json:5-10` 调 `powershell`，无 `validate.sh`） | **做（P0）**：validate 编排改由 Python 承担，npm scripts 改调 `python`；CI 三平台矩阵 |
| 硬编码个人路径 | 属实（`README.md:239-250`、`validate.ps1:28`） | **做（P0）**：清除所有 `C:\Users\litianyi`、`nvm4w` 字面量；claude 仅从 PATH 或文档化的环境变量解析 |
| 无 CI/CHANGELOG/CONTRIBUTING | 属实（未见 `.github/workflows`、`CHANGELOG`、`CONTRIBUTING`） | CI **做（P0，它是其余 P0 的强制手段）**；CHANGELOG/CONTRIBUTING **做（P1）** |

**LICENSE 决策——为什么是 Apache-2.0 而不是 MIT：** 客户的原话是"任何组织的合规流程都无法采用一个无许可证的依赖"。既然痛点是**组织采用**，就该选对企业合规最友好的那个：Apache-2.0 带显式专利授权，是企业法务最容易放行的宽松许可。仓库内 skills/templates 均为原创、无第三方代码需要额外署名，加 Apache-2.0 无阻碍。**决定：Apache-2.0，P0 落地。**

### 第四节 · 不足 5：已安装项目没有升级路径

**接受程度：完全接受。**

**我方分析：** 已核实。`init_project.py:68-77` 对已存在文件一律跳过（除非 `--force` 全量覆盖），生成文件无版本字段，无 migrate/diff。模板在上游演进后，已初始化项目确实无迁移机制。

**一处更正（不改变结论）：** 客户称模型名"烤死"在 "framework.md 和 cost-context-policy 模板"里。核实后：`framework.md:133-134,292-293` 与 `SKILL.md:392-394` 确实硬编码了 `gpt-5.5-medium`、`Sonnet`；但**安装进项目的** `cost-context-policy-template.md`（`:51-65`）的 Model Policy 段其实是空字段，并没有硬编码。也就是说，过时风险在于**规格文档**，不在于**分发给用户的模板**——这让问题稍微好一点点，但结论不变：模型名散落在多处纯文本、半衰期以月计、且无推送机制。

**决策（做 / 何时做）：**
- **做（P1）**：生成文件写入 `harnessloop_template_version` 字段；提供 `harnessloop migrate` 的 **drift 报告**能力（列出已安装文件与当前模板的差异）。
- **做（P1）**：把散落的模型名收敛到单一维护点（一张模型策略表），规格文档改为引用而非内联。
- **不做（划为 P2）**：不做"自动改写用户文件"的迁移。理由：协议文件多为用户手写，静默覆盖是危险操作——我们只报告漂移、提供选择性 `--force`，不替用户做主。
- **不做（P2）**：动态/远程模型策略解析暂不做，1.0 用"集中 + 版本化"即可。

### 第四节 · 不足 6：token 经济学没有被测量

**接受程度：完全接受。**

**我方分析：** 客户的讽刺完全成立：`cost-context-policy.md` 是协议钦定的设置文件（要求每个项目声明预算），而框架自己从未给出任何数字——一轮标准循环的协议开销是多少 token？相比裸奔放大了百分之几？换回多少返工？"重协议值得"目前的确只是一个信念。核实无误：委派矩阵反复以 "saves main-session context" 为价值（`SKILL.md:404-411`），却零测量。

**决策（做 / 何时做）：**
- **做（P1，与 dogfooding 绑定）**：用 1.0 自身开发作为第一个真实项目，实测并公布：每轮协议开销（token）、相对裸会话的放大比、以及至少一个"协议抓住将错就错、避免返工"的实证。没有数字之前，不再对外宣称"重协议是值得的"。

### 第五节 · 思辨 1：自指困境——evidence-backed 框架的 evidence-free 处境

**接受程度：完全接受。这是对我们最狠、也最该感谢的一击。**

**我方分析：** 已核实——仓库根无 `.harnessloop/`，唯一示例是人造 mock，`evals/matrix.md` 只有样例值。一个要求"一切断言附证据"的框架，对自身有效性零证据。客户那句"框架最大的缺失证据，是它自己的使用证据"，是整份报告我最认可的一句。

**决策（做 / 何时做）：**
- **做，立即启动（本周）**：对 Harnessloop 仓库自身执行 dogfooding——在仓库根建 `.harnessloop/`，把"交付 1.0"当作第一个真实、长期、跨会话的目标来管理。这一个动作同时解决三件事：补上使用证据、产出思辨 6 要的 token 数字、实测思辨 5 的升级路径。
- 验收：1.0 发布时，仓库根 `.harnessloop/` 至少有一个真实 goal、≥2 个真实 round、真实证据（CI 日志），且**至少一轮是 negative**——用真实失败证明协议在失败时运转，而不是又摆拍一个成功案例。

### 第五节 · 思辨 2：信任悖论——出路是下沉

**接受程度：完全接受。已在不足 2 作为架构承诺采纳。**

客户的二分法（判断类留给模型、机械类下沉为工具）我方全盘采纳，并已升格为 1.0 架构原则："代码保证下限，模型负责上限"。README 的 Current Limits 已诚实承认 "does not yet provide a full shell CLI"（`README.md:255`）——1.0 不是要补一个大而全的 CLI，而是先补上那把"30 行校验脚本"级别的机械 gate。不重复展开，见第三节 P0。

### 第五节 · 思辨 3：这是一个产品，还是一篇论文？

**接受程度：部分接受。**

**我方分析：**
- 接受的部分：当前形态**确实**规格重、可执行代码轻（近 500 行 `SKILL.md` 对不足 300 行可执行代码），使用证据为零。这个事实我方承认。
- 保留的部分：我**不接受**"v0.9 阶段规格重于代码 = 失败"这个隐含判断。**先把协议想清楚、再下沉为强制，是正确的工程顺序，不是错误的顺序。** 反过来才危险——在协议语义还没稳定前就烤死进脚本，会锁死错误的抽象。所以现状不是"论文冒充产品"，而是"一个还没走完下沉这一步的、顺序正确的早期产品"。

**决策（做 / 何时做）：** 我把客户提出的那个判据直接采纳为**内部对赌指标**：
> "下一批 commit 是在增加规范文字，还是在增加可执行的 gate。"
- **做**：1.0 里程碑内，可执行代码行数与机械 gate 覆盖数必须显著增长；这是可证伪的。若 1.0 发布时这条不达标，就是我作为 CTO 的失职，而不是含糊过去。战略定位的完整回答见第四节。

---

## 三、1.0 路线图决策（CTO 视角）

原则：**P0 = 严肃采用的信誉底线，没有它 1.0 不发；P1 = 兑现"下沉"承诺与消除自指困境；P2 = 明确推迟，给出理由。**
时间基线：今日 2026-07-02。目标 1.0-rc：2026-07-16；1.0 GA：2026-07 月底。

### P0（进 1.0，阻塞发布）

| # | 行动 | 验收标准 |
| --- | --- | --- |
| P0-1 | 加 LICENSE（Apache-2.0） | 仓库根有 `LICENSE`；两个 plugin manifest 含 SPDX 标识；marketplace 元数据引用许可证 |
| P0-2 | 跨平台 validate | validate 编排迁至 `scripts/validate.py`（Python 已是硬依赖）；`validate.ps1`/新增 `validate.sh` 仅作瘦包装；`package.json` 不再出现 `powershell` 字面量；CI 在 Windows + macOS + Linux 三平台 `npm run validate` 全绿 |
| P0-3 | 清除硬编码个人路径 | 全仓库 grep `C:\\Users`、`litianyi`、`nvm4w` 命中为 0；README 的 Validate 段改用相对/文档化路径；`validate.ps1` 的 claude 仅从 PATH 或环境变量解析 |
| P0-4 | 文档单一事实源 | `init_project.py` 的 `BASE_DIRS`/`BASE_FILES` 为唯一权威源；新增校验：任一文档骨架（README/usage/framework/SKILL）与之偏离即失败；README 补 `intake/`+`evals/`，usage.md 补 `current.md`+`self-check.md`+`matrix.md`；该校验并入 `validate` |
| P0-5 | `harnessloop verify` MVP（下沉第一刀） | 跨平台脚本；至少强制两条机械 gate：(a) 某轮 evidence/review 写入路径必须落在该轮 `scope-lock.md` 白名单内，越界即非零退出；(b) 评审文件引用的证据路径必须存在，缺失即失败；对 `examples/mock-project` 运行通过，且能被人为构造的越界/悬空引用触发失败；并入 CI |
| P0-6 | CI（其余 P0 的强制手段） | GitHub Actions 三平台矩阵，跑 `validate` + `verify` + 文档一致性校验；PR 未过 CI 不可合并 |

P0-5 与 P0-6 虽超出客户明确点名的四项，但我判定它们是信誉底线的一部分：没有 CI，前四项会立刻再次漂移；没有 `verify` MVP，我们对"信任悖论"的回应就仍停留在嘴上。故一并进 P0。

### P1（进 1.0 或紧随其后的 1.1，8 月内）

| # | 行动 | 验收标准 |
| --- | --- | --- |
| P1-1 | 对自身 dogfooding（立即启动） | 仓库根 `.harnessloop/` 有 ≥1 真实 goal、≥2 真实 round、真实 CI 证据、≥1 轮 negative |
| P1-2 | token 经济学实测 | 从 dogfooding 项目产出并公布：每轮协议 token 开销、相对裸会话放大比、≥1 个避免返工的实证 |
| P1-3 | 最小循环 quickstart + profile 分档 | `docs/quickstart.md` ≤10 步走完一轮；协议文档标注 `lite`/`standard`/`strict` 各 gate 是否可省 |
| P1-4 | 升级路径（drift 报告） | 生成文件含 `harnessloop_template_version`；`harnessloop migrate` 能列出已装文件与当前模板差异 |
| P1-5 | 模型名收敛 + CHANGELOG/CONTRIBUTING | 模型名集中到单一策略表，规格文档改引用；`CHANGELOG.md` 覆盖 0.9→1.0；`CONTRIBUTING.md` 就位 |
| P1-6 | verify 扩面 | status 只读性（零文件变更）、状态文件一致性纳入 `verify` |

### P2（明确推迟，附理由）

| # | 行动 | 推迟理由 |
| --- | --- | --- |
| P2-1 | 完整 shell CLI | **不做进 1.0。** 协议优先定位下，重 CLI 会绑死单一运行时、重复 skills 已表达的语义。1.0 只交付 `verify` 这把机械尺，不做别的。 |
| P2-2 | 自动改写用户文件的迁移 | **不做。** 协议文件多为用户手写，静默覆盖危险；只提供 drift 报告 + 选择性 `--force`。 |
| P2-3 | 动态/远程模型策略解析 | 推迟。需先有稳定的策略文件格式；1.0 用"集中 + 版本化"够用。 |
| P2-4 | 带真实执行结果的 eval 矩阵 | 推迟至 dogfooding 产出真实场景后再填，避免又造一批样例值。 |

---

## 四、战略定位决策（CEO 视角）

客户逼问："这是一个产品，还是一篇论文？"并给了一个二选一：协议优先，还是工具优先。我给明确答案，不打太极。

### 定位：协议优先，但强制一个可执行的下限。

**为什么不是工具优先：** Harnessloop 唯一真正的护城河，是它作为**纯项目本地文件协议**能同时跑在 Codex 和 Claude Code 上、不依赖任何单一厂商的 hook 机制。一旦转向工具优先、把核心逻辑塞进某一家的 CLI/hook，这条护城河当场消失，我们就退化成"某个 runtime 的又一个插件"。所以协议优先是商业上的正确选择，不是技术惰性。

**为什么协议优先不等于放任文字：** 客户的"信任悖论"是对的——纯文字协议把全部保障压在它最不信任的载体（LLM 指令遵循）上。答案不是放弃协议优先，而是给协议加一个**代码保证的下限**：机械可判的规则（P0-5 起步）必须由跨平台脚本强制，模型只负责需要判断的上限。**一句话定位：Harnessloop 是一个协议优先、但下限由代码兜底的长任务 agent 操作规范。**

### 目标用户：收窄，并写进文档。

- **保留并聚焦**：运行高风险、多天、跨会话 agent 任务、愿以重协议换完整审计轨迹的团队；以及 agent 框架设计者（当思想库读）。
- **明确劝退**：日常编码任务用户。README 已经在做（`README.md:34-38`），1.0 里说得更硬——这不是流失用户，这是拒绝错误用户，避免"协议开销 > 收益"的差评稀释口碑。
- **决策**：不为了扩大 TAM 而软化定位。宁可要一个窄而深、有人愿意为审计性付费的市场，不要一个宽而浅、装上就卸的市场。

### Dogfooding：可行，且立即启动（本周）。

- **可行性**：完全可行。1.0 开发本身就是一个真实的、长期的、跨会话的、高风险（要重构强制机制）的任务——正是 Harnessloop 声称最擅长的场景。没有比这更合适的第一个真实项目。
- **启动时间**：本周，不等 1.0 做完。因为它是一石三鸟：补使用证据（解思辨 1）、产 token 数字（解不足 6）、实测升级路径（解不足 5）。
- **这是本次最重要的一个战略决策**：dogfooding 把"我们相信协议有用"变成"这里是协议管理自己开发的全部 scope-lock、证据和决策文件，包括它抓住我们自己的三次将错就错"。这份记录一旦存在，就是比任何营销都有力的推销——也是对客户"框架最大的缺失证据是它自己的使用证据"这一击的唯一诚实回应。

---

## 五、对客户的致谢与后续

**最有价值的三个发现（按对路线的影响排序）：**
1. **"信任悖论：出路是下沉"** —— 它给了我们 1.0 的架构主线（代码保证下限、模型负责上限），并直接催生 P0-5 的 `verify` MVP。这是本次评审对产品走向影响最大的一条。
2. **"自指困境：框架最大的缺失证据是它自己的使用证据"** —— 它促成了立即启动 dogfooding 的决定，这是本次最重要的战略动作。
3. **"validate 检查 manifest 却不检查文档一致性"** —— 这一条精准到点出了漂移的机制根因，直接定义了 P0-4 的验收标准。

**我们特别欣赏的方法：** 客户完全按照 Harnessloop 自己第 13 条原则来评审——每一条论断都回到文件路径、可复核。这让我们无法用"你误解了"来搪塞，只能回到代码里认账或更正。这正是我们希望这个框架在世界上引发的那种行为。

**后续（做 / 何时做）：**
- **做，立即**：正式邀请该客户作为 1.0 的**外部对抗性评审人**（adversarial reviewer）——这恰好是 Harnessloop 委派矩阵里"必须委派、避免自我评审偏见"的角色（`SKILL.md:409`）。我们请他在 1.0-rc（目标 2026-07-16）时，用同一套"附证据路径"的标准复评 P0 六项是否真的落地。
- **做，随 1.0 发布**：公开本反馈文档与 1.0 的 dogfooding 记录，让外部能核对我们是否兑现了这里的每一个"做/何时做"。
- **承诺**：下一批 commit 会用"增加可执行 gate 多于增加规范文字"来回答客户那个判据。若 1.0 发布时未做到，责任在 CTO，记录在此，可被追究。

---

*本文引用并独立核实的关键文件*

| 结论 | 证据路径 |
| --- | --- |
| 唯一协议级硬断言（secrets 值为 null） | `scripts/validate.ps1:115-154`（尤其 `:151-153`） |
| scope-lock / 只读 status 仅为文字 | `plugins/harnessloop/skills/harnessloop-loop/SKILL.md:343-351`、`:163-181` |
| README 骨架缺 intake/、evals/ | `README.md:100-118` 对照 `init_project.py:16-24` |
| usage.md 初始化清单缺三文件 | `docs/usage.md:47-56` 对照 `init_project.py:26-36` |
| skip-unless-force、无版本字段 | `plugins/harnessloop/skills/harnessloop-loop/scripts/init_project.py:68-77` |
| npm scripts 仅 Windows | `package.json:5-10`；`scripts/` 无 `validate.sh` |
| 硬编码个人路径 | `README.md:239-250`、`scripts/validate.ps1:28` |
| 模型名硬编码（出处更正） | `docs/harnessloop-framework.md:133-134,292-293`、`SKILL.md:392-394`；模板 `cost-context-policy-template.md:51-65` 实为空字段 |
| 无 LICENSE / 无仓库自身 .harnessloop | glob `**/LICENSE*`、`.harnessloop/**` 均无命中 |
| 失败示例 / 自我批评 / 样例 eval | `examples/mock-project/.../rounds/0001/decision.md`、`.../meta/evolution-issues/0001-validation-drift-template-gap.md`、`.../evals/matrix.md` |
