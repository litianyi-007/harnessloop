interface:
  display_name: "Harnessloop Loop"
  short_description: "Run evidence-backed goal loops"
  default_prompt: "Use $harnessloop-loop to run or continue an evidence-backed Harnessloop goal."

policy:
  allow_implicit_invocation: true
